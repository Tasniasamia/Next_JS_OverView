import React from 'react';

const page = () => {
    console.log(process.env)
    return (
        <div>
            This is the home page into app router
        </div>
    );
};

export default page;