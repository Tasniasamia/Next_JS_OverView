import React from 'react';
export const metadata = {
    title: 'Login',
    description: 'Login here',
  }
   
const page = () => {
    return (
        <div style={{fontFamily:"Open Sans"}}>
            Login
        </div>
    );
};

export default page;